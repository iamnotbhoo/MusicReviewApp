<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/black"
    android:padding="16dp">

    <!-- Your existing SearchInputMobile component -->
    <student.projects.musicreviewapp.components.searching.SearchInputMobile
        android:id="@+id/search_input_component"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="24dp" />

    <!-- You can add your FilterResults component here later -->
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Search results will appear here"
        android:textColor="@color/grey_400"
        android:textSize="16sp"
        android:gravity="center"
        android:padding="32dp" />

</LinearLayout>